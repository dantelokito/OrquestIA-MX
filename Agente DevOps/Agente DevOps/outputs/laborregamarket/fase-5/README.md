# Fase 5 — CI, env OSM y handoff

Producto v0.5.0 (Leaflet/OSM, catálogo inhabilitado, marca PROVIDER).

**Docker/GHA e incidencias P0/P1** quedan para **Fase 6**. Backlog: [`../comun/deuda-fases-previas.md`](../comun/deuda-fases-previas.md).

Artefactos de esta fase (cuando se implementen): `docker/`, `.github/workflows/`, `infra-handoffs/`.

**No implementar aquí:** pasarela, CFDI, PWA, ECS, K8s, Prometheus. **No** exigir Maps JS key para Explorar.
